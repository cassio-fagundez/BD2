USE psdb;

/* 6. Consultando a tabela empregados (employees), selecione os nomes (first_name) distintos. Ordene pelo campo nome (first_name) de forma ascendente. */

SELECT DISTINCT first_name FROM employees ORDER BY first_name ASC;